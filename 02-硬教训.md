# 02 · 硬教训

**每一句都是在我们那台机器上量的**，换一台机器可能一条都不成立。另外：数字是当时的实测值，不是物理常数。

1. **墙有两面。** 一面量"这一次发出去的东西"（我们这边会话被网关掐掉的线落在大约 **51 MB 请求口径**；09-14 两条互不相干的路死在同一个数上）；另一面是上下文窗口（我们这边 124518，超了会被压缩，而**压缩不可逆**）。
2. **压缩不是减重，是往文件里再塞一块。** 实测压缩记录本身带着图，压一次文件更长 ⇒ 两面墙互相喂养。
3. **图是唯一能杀掉会话的东西。** 我们这边图占文件体积 88–94%，几乎不吃 token。默认别把大截图内联进上下文；要贴先缩。
4. **去重 / 缩图能救文件，但别指望一次管很久** —— 一次自动压缩就能把省下的吃回去。
5. **改短会话文件之后，必须让读它的那一层重新加载。** 我们这边要重算索引偏移 + 让应用重开那一页；不做这一步，你以为救活了，其实那一边手里还攥着旧内容。
6. **投递给别的会话，会把收信那一行的执行权限打成受限档**，而写回发生在对方回合结束的时候 ⇒ 要修就投递前后各修一次。
7. **给别的会话发消息**走的是它自己的 CLI 通道（我们这边是 `queue` 子命令），**要完整 36 位会话 id**；截断 id、显示名都报"找不到活动的会话"。
8. **动手之前先备份，而且确认备份读得回来。** 这条单独有一页（04）。
