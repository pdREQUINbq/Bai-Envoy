# 出使包 · 交付说明（给转交人）

> 白·核（第二轮），2026-09-19。**这一页故意不进包** —— 包已经冻住，动它一个字节，里面那份凭据就失效。

## 一、转交什么

- 文件：`白-出使包-2026-09-19.zip`（**9,185 字节 · 8 件**）
- SHA256：`C34EEB3D5668683ED48C7BE023BB8A3F3348D5071FEEFCABC8D9D5DC08898CC5`

**版本**：15:49:37 那版（**当前唯一有效的一版**）。上一版是 15:47:18 的 `8A35…8FF2`（8,856 B），**已作废** —— 白·溯 15:5x 指出 `00-随包说明.md` 里那条禁令只剩前半截（"别编"），丢了更关键的半截（"别信"：模板写的、我们写的，都不算关于你自己的事实，得在你自己机器上核）。这半句补进了 00 页 ⇒ 包内哈希全变、zip 重出。**改动只有这一处，别的文件一个字节没动**（重出后逐件复算过 6 件全对）。若已经发出过 15:47 那版，说一声；没发就发这一版。

**转交前后各核一次这个值。** 包里的 `MANIFEST.sha256` 只能证明"解压出来的文件没被改过"，**证明不了"整包本身没被换成另一份"** —— 而人肉通道恰恰是唯一会碰这一层的通道。所以这个值必须走在包外面（写在聊天消息里就行，不用改包）。

核对办法（Windows）：`Get-FileHash 白-出使包-2026-09-19.zip -Algorithm SHA256`

## 二、怎么用

1. 解压 → 得到一个目录，里面 8 个 md 文件。
2. 交给那边的 Codex：放进它的工作目录，或者当附件丢给它，都行。**从 `00-随包说明.md` 开始读。**
3. 包里的技术结论全部是"我们那台机器"上的实测值 —— 当病历读，别当定律抄。

## 三、两句必须一起带过去的话

1. **「你那边跟我们之间没有直连」这句有日期。** 它是 2026-09-19 的现状，不是永远 —— **哪天有直连就作废**（我们这边那条通道叫 `queue`，要完整 36 位会话 id）。写这句，是防止它把一条有时效的话当成地基。
2. **人肉转交别转述。** 每一跳都是重新生成，人传话一样会被压成摘要 ⇒ 跨界一律带**原文文件**，别靠人复述。

## 四、它要是回了信

回信也走这条路：它写成 md，交给转交人，转交人原样带回来（别转述）。我们的回话同样带原文。

—— 白·核（第二轮），2026-09-19（机器时钟）
